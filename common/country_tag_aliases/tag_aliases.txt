
# tag aliases are country tags that maps to different countries depending on a set of triggers

#TAG = { #name of alias tag. must be 3 letters and different from any other tags. once this is defined, it will be available in any script that uses country tags
#
#	# you can map an alias directly to an event target or another variable. only use one of the following entries
#	# if any of these are used, the alias will be mapped to that specific variable/event target and no other check will be done
#	variable = var_name
#	global_event_target = event_target_name
#	event_target = event_target_name
#
#	# if an alias is not mapped directly to a variable/event target, it will be searched within a list of countries that can be definied below
#	# you can use any combination of these below
#	# while checking for triggers/variables, current scope will be the country we are checking and PREV will be the scope that uses the alias
#	# if none of the following targets are defined, the game will check all existing countries but this is highly discouraged for performance reasons
#	original_tag = GER # pick a country that has original_tag of GER
#	targets = { GER FRA ENG } # search within the list of tags/variables
#	target_array = array_name # search within an array
#
#	# by default alias will return the first existing country that it matches and fulfils the triggers. you can also define a scoring system like this and it will pick the tag with the highest score > 0
#	country_score = {
#		base = 1
#		modifier = {
#			add = 10
#			# if mapped tag is in same faction with the country in scope that calls this alias give it a higher score
#			is_in_faction_with = PREV
#		}
#		modifier = {
#			add = 5
#			# if the mapped tag has fascism give it a higher score
#			has_government = fascism
#		}
#	}
#
#	fallback = GER # if the alias can't match anything, use this one as fallback (no triggers or weight will be checked)
#
#	# you can define a list of trigger, in that case it will chech is tags that this alias is mapped to and return only if the triggers are true
#	or = {
#		#only accept targets that is ally with us or is fascist
#		is_in_faction_with = PREV
#		has_government = fascism
#	}
#	# trigger 2
#	# trigger 3
#	# ...
#}

# operative operation target
# only set for majors who has agency
# set on on_weekly
MOT = {
	variable = generic_operation_target
}

##### These are added to remove some error messages. Should be less heavy than actually adding those tags back.
GER = {
	original_tag = OOO # OLA
}

ENG = {
	original_tag = OOO # IZL
}

IRE = {
	original_tag = OOO # IIE
}

FRA = {
	original_tag = OOO # PJM
}

ITA = {
	original_tag = OOO # OOZ
}

SOV = {
	original_tag = OOO # ZIY
}

TAN = {
	original_tag = OOO # EZY
}

MON = {
	original_tag = OOO # EEO
}

JAP = {
	original_tag = OOO # ZBZ
}

MAN = {
	original_tag = OOO # EZB
}

MEN = {
	original_tag = OOO # LII
}

CHI = {
	original_tag = OOO # LIE
}

PRC = {
	original_tag = OOO # LZZ
}

SND = {
	original_tag = OOO # NEB
}

HBC = {
	original_tag = OOO # LOB
}

SHX = {
	original_tag = OOO # LIS
}

XIC = {
	original_tag = OOO # FAA
}

SIC = {
	original_tag = OOO # NEF
}

XSM = {
	original_tag = OOO # GTB
}

GSM = {
	original_tag = OOO # NAA
}

NXM = {
	original_tag = OOO # LIL
}

SIK = {
	original_tag = OOO # LIF
}

KHM = {
	original_tag = OOO # NAZ
}

YUN = {
	original_tag = OOO # EZS
}

GXC = {
	original_tag = OOO # SYA
}

GDC = {
	original_tag = OOO # SYZ
}

TIB = {
	original_tag = OOO # FSF
}

USA = {
	original_tag = OOO # ELI
}

POL = {
	original_tag = OOO # OIO
}

FIN = {
	original_tag = OOO # III
}

EST = {
	original_tag = OOO # BIZ
}

LAT = {
	original_tag = OOO # BOB
}

LIT = {
	original_tag = OOO # OII
}

ROM = {
	original_tag = OOO # OAL
}

NOR = {
	original_tag = OOO # IIO
}

SWE = {
	original_tag = OOO # IAI
}

DEN = {
	original_tag = OOO # OEF
}

HOL = {
	original_tag = OOO # OOF
}

BEL = {
	original_tag = OOO # OOL
}

LUX = {
	original_tag = OOO # OOB
}

AUS = {
	original_tag = OOO # OOA
}

HUN = {
	original_tag = OOO # OAE
}

CZE = {
	original_tag = OOO # OOY
}

SLO = {
	original_tag = OOO # OFO
}

YUG = {
	original_tag = OOO # IOF
}

ALB = {
	original_tag = OOO # OAA
}

DDR = {
	original_tag = OOO # ASE
}

WGR = {
	original_tag = OOO # OAZ
}

AST = {
	original_tag = OOO # ZBS
}

NZL = {
	original_tag = OOO # ZBA
}

CHL = {
	original_tag = OOO # ZFY
}

ARG = {
	original_tag = OOO # ZFB
}

PAR = {
	original_tag = OOO # EOI
}

URG = {
	original_tag = OOO # EOO
}

BRA = {
	original_tag = OOO # SOO
}

BOL = {
	original_tag = OOO # EOZ
}

PRU = {
	original_tag = OOO # EOE
}

ECU = {
	original_tag = OOO # EOS
}

COL = {
	original_tag = OOO # EOL
}

VEN = {
	original_tag = OOO # EOF
}

DOM = {
	original_tag = OOO # EIY
}

HAI = {
	original_tag = OOO # EIB
}

CUB = {
	original_tag = OOO # EIS
}

GUA = {
	original_tag = OOO # EIE
}

HON = {
	original_tag = OOO # EIZ
}

ELS = {
	original_tag = OOO # EIA
}

NIC = {
	original_tag = OOO # EIF
}

COS = {
	original_tag = OOO # EIL
}

PAN = {
	original_tag = OOO # EOA
}

AFG = {
	original_tag = OOO # ZLF
}

PER = {
	original_tag = OOO # ZLL
}

IRQ = {
	original_tag = OOO # ZYI
}

KUW = {
	original_tag = OOO # LSL
}

SYR = {
	original_tag = OOO # SSA
}

LEB = {
	original_tag = OOO # SSE
}

PAL = {
	original_tag = OOO # ASA
}

JOR = {
	original_tag = OOO # ASS
}

SAU = {
	original_tag = OOO # ZYZ
}

YEM = {
	original_tag = OOO # ZYE
}

OMA = {
	original_tag = OOO # ZYA
}

TUR = {
	original_tag = OOO # OAY
}

GRE = {
	original_tag = OOO # OAF
}

BUL = {
	original_tag = OOO # OAB
}

PRE = {
	original_tag = OOO # FLE
}

SPR = {
	original_tag = OOO # OAI
}

SPA = {
	original_tag = OOO # IFI
}

SPB = {
	original_tag = OOO # IFL
}

SPC = {
	original_tag = OOO # ILS
}

SPD = {
	original_tag = OOO # ILY
}

POR = {
	original_tag = OOO # IIZ
}

MEX = {
	original_tag = OOO # ZFF
}

ICE = {
	original_tag = OOO # IOO
}

SWI = {
	original_tag = OOO # OOE
}

ETH = {
	original_tag = OOO # ZFI
}

AFA = {
	original_tag = OOO # YOB
}

COG = {
	original_tag = OOO # ZYS
}

LIB = {
	original_tag = OOO # ZYB
}

CAN = {
	original_tag = OOO # ZFL
}

SAF = {
	original_tag = OOO # ZFS
}

RAJ = {
	original_tag = OOO # AEY
}

NEP = {
	original_tag = OOO # EZE
}

BHU = {
	original_tag = OOO # EZA
}

BAN = {
	original_tag = OOO # AEO
}

BRM = {
	original_tag = OOO # YYS
}

SIA = {
	original_tag = OOO # ZBY
}

MAL = {
	original_tag = OOO # EEL
}

INS = {
	original_tag = OOO # EES
}

PHI = {
	original_tag = OOO # EZF
}
